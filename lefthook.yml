pre-commit:
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,css,json,md}"
      run: bunx prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx eslint {staged_files}

pre-push:
  commands:
    typecheck:
      run: bunx tsc --noEmit
